{
  "name": "@gymbro/ui-kit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "_sideEffectsNote": "Must stay true: the kit's styles are .less, so a glob like **/*.css marks them side-effect-free and Rollup drops tokens.less from the prod build, leaving every var(--gb-*) unresolved.",
  "sideEffects": true,
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
